{
  "name": "smartcart-local-demo-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Local/demo SmartCart HTTP service with an optional server-side Instacart handoff.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  }
}
